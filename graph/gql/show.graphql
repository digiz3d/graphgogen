type Show {
  id: ID!
  name: String!
  description: String
  user: User!
}

extend type Query {
  show(id: ID!): Show!
}

extend type Mutation {
  createShow(name: String!, description: String!, userId: ID!): Show!
}
